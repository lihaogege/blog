import React from 'react';
import classes from "./Logo.module.less"
const Logo = () => {
    return (
        <div className={classes.logo}>
            主页
        </div>
    );
};

export default Logo;